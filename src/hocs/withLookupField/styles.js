import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textInput: {
    height: 40,
    borderColor: '#aaa',
    borderBottomWidth: 1,
    marginHorizontal: 10,
    paddingHorizontal: 0
  }
});